// Clinical Trial Context Graph — selected, public ClinicalTrials.gov records.
// CANVAS_HEIGHT: 660
const trialNodes = [
  { id: 'sponsor', label: 'Otsuka-affiliated\npublic records', type: 'SPONSOR', x: -420, y: 0, detail: 'Selected records list an Otsuka entity as sponsor or responsible party. This is a curated public-record sample, not a portfolio-wide sponsor census.' },
  { id: 'nct04030143', label: 'NCT04030143\nMultiple-dose aripiprazole', type: 'STUDY', x: -100, y: -240, source: 'https://clinicaltrials.gov/study/NCT04030143', detail: 'Completed study; conditions: schizophrenia and bipolar I disorder; intervention: aripiprazole; sponsor: Otsuka Pharmaceutical Development & Commercialization, Inc.' },
  { id: 'nct01438060', label: 'NCT01438060\nAripiprazole in dementia psychosis', type: 'STUDY', x: -100, y: -80, source: 'https://clinicaltrials.gov/study/NCT01438060', detail: 'Completed study; condition: dementia, Alzheimer type; interventions: aripiprazole and placebo; sponsor: Otsuka Pharmaceutical Development & Commercialization, Inc.' },
  { id: 'nct03179787', label: 'NCT03179787\nAripiprazole post-marketing', type: 'STUDY', x: -100, y: 80, source: 'https://clinicaltrials.gov/study/NCT03179787', detail: 'Completed post-marketing surveillance study; condition: autism; sponsor: Otsuka Pharmaceutical Co., Ltd.' },
  { id: 'nct07075328', label: 'NCT07075328\nOJP-001 in ATLL', type: 'STUDY', x: -100, y: 240, source: 'https://clinicaltrials.gov/study/NCT07075328', detail: 'Recruiting Phase I/II study in adult T-cell leukemia/lymphoma; intervention includes OJP-001 and 5-ALA; sponsor: Otsuka Medical Devices Co., Ltd. Japan.' },
  { id: 'aripiprazole', label: 'Aripiprazole', type: 'INTERVENTION', x: 225, y: -205, detail: 'Intervention named in the linked study records.' },
  { id: 'placebo', label: 'Placebo', type: 'INTERVENTION', x: 225, y: -70, detail: 'Comparator intervention named in NCT01438060.' },
  { id: 'ojp001', label: 'OJP-001 + 5-ALA', type: 'INTERVENTION', x: 225, y: 225, detail: 'Intervention context named in NCT07075328.' },
  { id: 'schizophrenia', label: 'Schizophrenia', type: 'CONDITION', x: 215, y: -330, detail: 'Condition named in NCT04030143.' },
  { id: 'bipolar', label: 'Bipolar I disorder', type: 'CONDITION', x: 420, y: -270, detail: 'Condition named in NCT04030143.' },
  { id: 'dementia', label: 'Dementia,\nAlzheimer type', type: 'CONDITION', x: 420, y: -80, detail: 'Condition named in NCT01438060.' },
  { id: 'autism', label: 'Autism', type: 'CONDITION', x: 390, y: 55, detail: 'Condition named in NCT03179787.' },
  { id: 'atll', label: 'Adult T-cell\nleukemia/lymphoma', type: 'CONDITION', x: 425, y: 180, detail: 'Condition named in NCT07075328.' },
  { id: 'completed', label: 'Completed', type: 'STATUS', x: -310, y: -185, detail: 'Status snapshot from linked public study records.' },
  { id: 'recruiting', label: 'Recruiting', type: 'STATUS', x: -315, y: 245, detail: 'Status snapshot from the linked public study record; recheck before operational use.' },
  { id: 'phase12', label: 'Phase I / II', type: 'PHASE', x: 105, y: 335, detail: 'Phase named in NCT07075328.' }
];
const trialEdges = [
  ['nct04030143', 'sponsor', 'SPONSORED BY'], ['nct01438060', 'sponsor', 'SPONSORED BY'], ['nct03179787', 'sponsor', 'SPONSORED BY'], ['nct07075328', 'sponsor', 'SPONSORED BY'],
  ['nct04030143', 'aripiprazole', 'STUDIES'], ['nct01438060', 'aripiprazole', 'STUDIES'], ['nct01438060', 'placebo', 'COMPARES'], ['nct07075328', 'ojp001', 'STUDIES'],
  ['nct04030143', 'schizophrenia', 'CONDITION'], ['nct04030143', 'bipolar', 'CONDITION'], ['nct01438060', 'dementia', 'CONDITION'], ['nct03179787', 'autism', 'CONDITION'], ['nct07075328', 'atll', 'CONDITION'],
  ['nct04030143', 'completed', 'STATUS'], ['nct01438060', 'completed', 'STATUS'], ['nct03179787', 'completed', 'STATUS'], ['nct07075328', 'recruiting', 'STATUS'], ['nct07075328', 'phase12', 'PHASE']
];
const trialColors = { SPONSOR: ['#4dd8d0', '#b7fffb'], STUDY: ['#65a9ff', '#d5e6ff'], CONDITION: ['#ad8cff', '#eadfff'], INTERVENTION: ['#ff9b7f', '#ffe0d6'], STATUS: ['#f6cd70', '#fff2b9'], PHASE: ['#70d99d', '#d9ffe8'] };
let trialNetwork, trialDataNodes, trialDataEdges, activeTrialFilter = 'ALL';
const h = value => String(value).replace(/[&<>"]/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' }[char]));
function buildTrials() {
  trialDataNodes = new vis.DataSet(trialNodes.map(node => { const [background, border] = trialColors[node.type]; return { ...node, color: { background, border, highlight: { background: border, border: '#fff' } }, shape: node.type === 'STUDY' ? 'box' : node.type === 'SPONSOR' ? 'hexagon' : 'ellipse', margin: 10, font: { color: '#06181d', size: node.type === 'STUDY' ? 12 : 13, face: 'Arial' }, borderWidth: 2 }; }));
  trialDataEdges = new vis.DataSet(trialEdges.map(([from, to, label], id) => ({ id, from, to, label, arrows: 'to', color: { color: 'rgba(170, 226, 220, .47)', highlight: '#fff' }, font: { color: '#dff7f4', size: 9, strokeWidth: 0 }, width: 1.4, smooth: { type: 'cubicBezier', roundness: .18 } })));
  const standalone = window.self === window.top;
  trialNetwork = new vis.Network(document.getElementById('network'), { nodes: trialDataNodes, edges: trialDataEdges }, { layout: { improvedLayout: false }, physics: false, interaction: { hover: true, dragView: standalone, zoomView: standalone, navigationButtons: true, selectConnectedEdges: false }, nodes: { shadow: { enabled: true, color: 'rgba(0,0,0,.35)', size: 8, x: 0, y: 3 } } });
  trialNetwork.once('afterDrawing', () => trialNetwork.fit({ animation: false, padding: 86, maxZoomLevel: 1 }));
  trialNetwork.on('selectNode', event => showTrialDetails(event.nodes[0]));
  trialNetwork.on('deselectNode', clearTrialDetails);
}
function showTrialDetails(id) { const node = trialNodes.find(item => item.id === id); document.getElementById('trial-details').innerHTML = `<h2>${h(node.label.replace('\n', ' '))}</h2><p>${h(node.type.toLowerCase())}</p><dl><dt>Public-record context</dt><dd>${h(node.detail)}</dd>${node.source ? `<dt>Primary source</dt><dd><a href="${h(node.source)}" target="_blank" rel="noreferrer">Open ClinicalTrials.gov record ↗</a></dd>` : ''}</dl>`; }
function clearTrialDetails() { document.getElementById('trial-details').innerHTML = '<h2>Select a node</h2><p>Source-backed trial context and a direct ClinicalTrials.gov record link will appear here.</p>'; }
function trialFilter() { const term = document.getElementById('trial-search').value.trim().toLowerCase(); const shown = new Set(trialNodes.filter(node => (activeTrialFilter === 'ALL' || node.type === activeTrialFilter) && `${node.label} ${node.detail}`.toLowerCase().includes(term)).map(node => node.id)); trialDataNodes.update(trialNodes.map(node => ({ id: node.id, hidden: !shown.has(node.id) }))); trialDataEdges.update(trialDataEdges.get().map(edge => ({ id: edge.id, hidden: !shown.has(edge.from) || !shown.has(edge.to) }))); }
function trialAsk() { const term = document.getElementById('trial-question').value.trim(); const result = document.getElementById('trial-result'); if (!term) { result.textContent = 'Type a study, condition, intervention, phase, or status.'; return; } document.getElementById('trial-search').value = term; activeTrialFilter = 'ALL'; document.querySelectorAll('#trial-filters .filter').forEach(button => button.classList.toggle('active', button.dataset.type === 'ALL')); trialFilter(); const matching = trialNodes.filter(node => `${node.label} ${node.detail}`.toLowerCase().includes(term.toLowerCase())); result.innerHTML = matching.length ? `<strong>${matching.length} matching context node${matching.length === 1 ? '' : 's'}</strong><br>Highlighted in the graph. Select a study node to inspect its linked public record.` : '<strong>No exact local match</strong><br>Try aripiprazole, recruiting, schizophrenia, autism, dementia, or Phase I.'; }
function initTrials() { buildTrials(); document.getElementById('trial-stats').innerHTML = [['study records', 4], ['typed links', trialEdges.length], ['conditions', trialNodes.filter(n => n.type === 'CONDITION').length], ['public sources', 4]].map(([label, count]) => `<div class="stat"><b>${count}</b><span>${label}</span></div>`).join(''); const filters = ['ALL', ...Object.keys(trialColors)]; document.getElementById('trial-filters').innerHTML = filters.map(type => `<button type="button" class="filter ${type === 'ALL' ? 'active' : ''}" data-type="${type}">${type === 'ALL' ? 'All types' : type.toLowerCase()}</button>`).join(''); document.querySelectorAll('#trial-filters .filter').forEach(button => button.addEventListener('click', () => { activeTrialFilter = button.dataset.type; document.querySelectorAll('#trial-filters .filter').forEach(item => item.classList.toggle('active', item === button)); trialFilter(); })); document.getElementById('trial-search').addEventListener('input', trialFilter); document.getElementById('trial-ask-button').addEventListener('click', trialAsk); document.getElementById('trial-question').addEventListener('keydown', event => { if (event.key === 'Enter') trialAsk(); }); document.querySelectorAll('[data-trial-question]').forEach(button => button.addEventListener('click', () => { document.getElementById('trial-question').value = button.dataset.trialQuestion; trialAsk(); })); document.getElementById('trial-reset').addEventListener('click', () => { document.getElementById('trial-search').value = ''; document.querySelector('#trial-filters .filter').click(); trialNetwork.fit({ animation: { duration: 250 }, padding: 86, maxZoomLevel: 1 }); clearTrialDetails(); }); }
document.addEventListener('DOMContentLoaded', initTrials);
